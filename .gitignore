# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Raw design sources — the Gemini renders, the logo lockups and the ~230 raw
# hero frames. These are inputs to tools/, not to the build: the optimised
# output that the site actually serves lives in public/assets/ and IS tracked.
# Kept out of git so a clone stays small; back them up separately.
/Martin logistics Assets/

# Visual check written by tools/map_dots.py
/tools/_mapcheck.png
